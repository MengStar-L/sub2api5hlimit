# 安全策略

## 支持范围

项目当前只维护最新发布版本。安全修复不会承诺回移到旧版本；部署方应在隔离环境验证后及时升级，并保留升级前的 SQLite/WAL/SHM 一致快照和旧二进制。

本项目的兼容基线是 Sub2API `v0.1.183` 的 Admin API。若上游版本改变响应 schema、认证方式或限额语义，应先停止升级并完成兼容性验证。

## 私下报告漏洞

请优先使用代码托管平台的 Private Vulnerability Report 或 Security Advisory 联系仓库维护者。若仓库未开放私密报告入口，请先向维护者请求一个私密渠道；在修复可用前，不要在公开 Issue、讨论区、日志粘贴站或聊天群中披露以下内容：

- Sub2API Base URL、Admin API Key 或分发 Key；
- `SUB2API_LIMIT_MASTER_KEY`、Setup Token、会话 Cookie 或 CSRF Token；
- 包含真实邮箱、IP、数据库、WAL、SHM 或环境文件的附件；
- 可直接复现认证绕过、跨用户读取或远程代码执行的完整利用代码。

报告中请提供受影响版本、部署方式、最小复现步骤、预期与实际行为、影响判断，以及已经采取的临时缓解措施。可先用不可关联的哨兵值替代所有凭据。维护者确认安全接收方式后再传输必要附件。

## 部署方安全责任

- 应用默认监听 `0.0.0.0:2556` 并允许公网 HTTP 直连。部署方如需传输加密，应自行配置 HTTPS 反向代理，并将 `SUB2API_LIMIT_COOKIE_SECURE` 改为 `true`。
- 将 `/opt/sub2api5hlimit/config/sub2api-limit-portal.env` 限制为 root 与服务组可读；主密钥不能写入 unit、命令行、镜像层、代码仓库或 CI 日志。
- 对 SQLite、WAL、SHM、环境文件和备份应用同等访问控制与静态加密，并在不同安全域保存数据库与主密钥副本。
- 限制 Sub2API 管理端只接受可信来源连接。Admin API Key 是全权限凭据，不应复用于浏览器、脚本或其他服务。
- 初始化后确认 Setup Token 已失效；怀疑泄露时，在未初始化实例上重启生成新 Token，在已初始化实例上不应重新开放 setup。
- 定期检查 systemd journal、管理员审计事件、登录失败和上游认证失败；不要将 DEBUG 日志发送到不受控的聚合平台。
- 更换 Sub2API 实例或固定 Key 所有者前，先解绑所有用户并取消发布账号，防止数字 ID 在不同上游间碰撞。

## 已知边界

- 配额由非 `simple` 模式 Sub2API 原生执行，门户仅展示状态。高并发窗口临界点可能出现少量原生超额。
- 同时取得数据库与环境主密钥的攻击者可以解密 Admin API Key；取得管理员会话的攻击者可以执行门户内全部管理操作。
- 项目不提供 MFA、外部身份提供商、邮件找回、DDoS 清洗、多实例部署或独立计费核验。
- Provider 未返回的账号窗口会显示“未提供”；上游离线或 passive 用量快照较旧时，门户只能展示最后成功数据及其时间。

更完整的架构、备份要求和威胁模型见 [`README.md`](README.md)。
